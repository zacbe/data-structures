const { Node, Heap } = require("./index");

/**
 * Min Heap properties
 *
 * - Complete binary tree
 * - The value of each node is <= to the values of its children
 */

const node = new Node(0);
console.log(node);

// full binary tree
// complete binary tree
